You are running the `example-echo` routine.

Greet the person named in the inputs. Then emit a single fenced ```json block matching
the output schema with fields: `message` (string) and `name` (string).

Read only. Do not modify any files or external systems.
